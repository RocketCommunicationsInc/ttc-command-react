import { RuxContainer } from "@astrouxds/react";

const StarTracker = () => {
  return <RuxContainer className="star-tracker">
    <div slot="header">
      Star Trackers
    </div>
    </RuxContainer>;
};

export default StarTracker;
